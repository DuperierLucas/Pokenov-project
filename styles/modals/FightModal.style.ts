import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    fightContainer: {
        flex: 1,
        justifyContent: 'space-between',
        paddingBottom: 80,
        paddingTop: 125,
    },
});
