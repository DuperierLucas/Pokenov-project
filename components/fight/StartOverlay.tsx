import React from 'react';
import { Text, View } from 'react-native';

const StartOverlay = (): JSX.Element => {
    return (
        <View>
            <Text>Start</Text>
        </View>
    );
};

export default StartOverlay;
